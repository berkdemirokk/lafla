{
  "name": "lafla",
  "version": "0.1.0",
  "private": true,
  "description": "Söyle gitsin — bağlam temelli İngilizce hayatta kalma simülatörü.",
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "dev:mobile": "pnpm --filter mobile dev",
    "dev:api": "pnpm --filter api dev",
    "generate:content": "pnpm --filter scripts generate"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/node": "^20.10.0"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  }
}
